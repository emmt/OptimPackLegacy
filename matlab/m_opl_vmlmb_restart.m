% function m_opl_vmlmb_restart
%
%     task = m_opl_vmlmb_restart(ws)
%
% Restart a new optimization by VMLM-B with the same workspace and thus the
% same settings (problem size, number of memorized dimensions, tolerances,
% etc.).
